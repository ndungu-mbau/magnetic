services:
  postgres:
    image: postgres:18-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: magnetic_new_db
      POSTGRES_USER: neondb_owner
      POSTGRES_PASSWORD: npg_MWPhUV8JOB2t
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - '5432:5432'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U "$${POSTGRES_USER}"']
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres-data:
    name: magnetic-db-data
